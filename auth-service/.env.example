# Auth Service Configuration
# Copy this file to .env and update with your values

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server host and port
HOST=0.0.0.0
PORT=8001

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# SQLite database URL (default for development)
DATABASE_URL=sqlite:///./data/auth.db

# For PostgreSQL (production):
# DATABASE_URL=postgresql://user:password@localhost:5432/auth_db

# =============================================================================
# JWT CONFIGURATION
# =============================================================================

# JWT Secret Key - MUST BE CHANGED IN PRODUCTION
# Generate with: openssl rand -hex 32
AUTH_SECRET_KEY=your-super-secret-key-at-least-32-characters-long-change-in-production

# JWT Algorithm (HS256 recommended)
ALGORITHM=HS256

# Token expiration in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================================================
# ADMIN USER CONFIGURATION
# =============================================================================

# Default admin user credentials (created on first startup)
# IMPORTANT: Change these before first run for security!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@example.com

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Enable auto-reload for development
RELOAD=false

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Allowed origins for CORS (comma-separated)
CORS_ORIGINS=*

# =============================================================================
# STANDALONE RUNNING INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Update values (especially AUTH_SECRET_KEY)
# 3. Create data directory: mkdir -p data
# 4. Install dependencies: pip install -r requirements.txt (or poetry install)
# 5. Run: python -m uvicorn auth_server.main:app --host 0.0.0.0 --port 8001
#
# With Docker:
# docker build -t auth-service .
# docker run -p 8001:8001 --env-file .env -v $(pwd)/data:/app/data auth-service
