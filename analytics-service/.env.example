# Analytics Service Configuration
# Copy this file to .env and update with your values

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server host and port
HOST=0.0.0.0
PORT=8002

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# SQLite database URL
DATABASE_URL=sqlite:///./data/analytics.db

# =============================================================================
# AUTH SERVICE INTEGRATION
# =============================================================================

# Auth service URL for token verification
# Use Docker service name when running with docker-compose
AUTH_SERVICE_URL=http://auth-server:8001
# For standalone/local development:
# AUTH_SERVICE_URL=http://localhost:8001

# JWT Secret Key (MUST match auth-service)
AUTH_SECRET_KEY=your-super-secret-key-at-least-32-characters-long-change-in-production

# JWT Algorithm
ALGORITHM=HS256

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Allowed origins for CORS (comma-separated)
CORS_ORIGINS=*

# =============================================================================
# STANDALONE RUNNING INSTRUCTIONS
# =============================================================================
# Prerequisites: Auth service must be running first
#
# 1. Copy this file to .env: cp .env.example .env
# 2. Update AUTH_SECRET_KEY (must match auth-service)
# 3. Update AUTH_SERVICE_URL to point to running auth-service
# 4. Create data directory: mkdir -p data
# 5. Install dependencies: pip install -r requirements.txt
# 6. Run: python -m uvicorn main:app --host 0.0.0.0 --port 8002
#
# With Docker:
# docker build -t analytics-service .
# docker run -p 8002:8002 --env-file .env -v $(pwd)/data:/app/data analytics-service
