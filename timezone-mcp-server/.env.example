# Timezone MCP Server Configuration
# Copy this file to .env and update with your values

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server host and port
HOST=0.0.0.0
PORT=8003

# Service name for logging
SERVICE_NAME=timezone-mcp-server

# =============================================================================
# AUTH SERVICE INTEGRATION
# =============================================================================

# Auth service URL for token verification
# Use Docker service name when running in docker-compose
AUTH_SERVICE_URL=http://auth-server:8001
# For standalone/local development:
# AUTH_SERVICE_URL=http://localhost:8001

# JWT Secret Key (should match auth-service)
AUTH_SECRET_KEY=your-secret-key-here-change-this-in-production

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# STANDALONE RUNNING
# =============================================================================
# To run this service standalone:
# 1. Copy this file to .env
# 2. Update AUTH_SERVICE_URL to point to running auth-service
# 3. Run: python mcp_http_server.py
# Or with uvicorn: uvicorn mcp_http_server:app --host 0.0.0.0 --port 8003
