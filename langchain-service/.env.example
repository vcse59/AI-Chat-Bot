# LangChain Service Configuration
# Copy this file to .env and update with your values

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server host and port
HOST=0.0.0.0
PORT=8004

# Service name for logging
SERVICE_NAME=langchain-service

# =============================================================================
# OPENAI CONFIGURATION (Required)
# =============================================================================

# OpenAI API Key - Get yours at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# =============================================================================
# LANGCHAIN CONFIGURATION
# =============================================================================

# Model to use for LangChain workflows
LANGCHAIN_MODEL=gpt-3.5-turbo

# Model temperature (0.0-1.0, higher = more creative)
LANGCHAIN_TEMPERATURE=0.7

# Maximum tokens for responses
LANGCHAIN_MAX_TOKENS=1000

# Enable verbose logging for LangChain
LANGCHAIN_VERBOSE=false

# =============================================================================
# CHAT SERVICE INTEGRATION
# =============================================================================

# Chat service URL for workflow integration
# Use Docker service name when running with docker-compose
CHAT_SERVICE_URL=http://openai-chatbot:8000
# For standalone/local development:
# CHAT_SERVICE_URL=http://localhost:8000

# =============================================================================
# AUTH SERVICE INTEGRATION
# =============================================================================

# Auth service URL for token verification
# Use Docker service name when running with docker-compose
AUTH_SERVICE_URL=http://auth-server:8001
# For standalone/local development:
# AUTH_SERVICE_URL=http://localhost:8001

# JWT Secret Key (MUST match auth-service)
AUTH_SECRET_KEY=your-super-secret-key-at-least-32-characters-long-change-in-production

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# STANDALONE RUNNING INSTRUCTIONS
# =============================================================================
# Prerequisites: Auth service and Chat service should be running
#
# 1. Copy this file to .env: cp .env.example .env
# 2. Update OPENAI_API_KEY
# 3. Update AUTH_SECRET_KEY (must match auth-service)
# 4. Update service URLs to point to running services
# 5. Install dependencies: pip install -r requirements.txt
# 6. Run: python -m uvicorn main:app --host 0.0.0.0 --port 8004
#
# With Docker:
# docker build -t langchain-service .
# docker run -p 8004:8004 --env-file .env langchain-service
