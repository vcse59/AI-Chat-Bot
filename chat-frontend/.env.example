# React Frontend Configuration
# Copy this file to .env and update with your values

# =============================================================================
# BACKEND SERVICE URLs
# =============================================================================

# Auth service URL
REACT_APP_AUTH_API_URL=http://localhost:8001

# Chat service URL
REACT_APP_CHAT_API_URL=http://localhost:8000

# Analytics service URL (admin only)
REACT_APP_ANALYTICS_API_URL=http://localhost:8002

# LangChain service URL (for workflow templates)
REACT_APP_LANGCHAIN_API_URL=http://localhost:8004

# =============================================================================
# WEBSOCKET CONFIGURATION
# =============================================================================

# WebSocket URL for real-time chat
REACT_APP_WS_URL=ws://localhost:8000

# =============================================================================
# STANDALONE RUNNING INSTRUCTIONS
# =============================================================================
# Prerequisites: Backend services (auth, chat) must be running
#
# 1. Copy this file to .env: cp .env.example .env
# 2. Update URLs if services are running on different hosts/ports
# 3. Install dependencies: npm install
# 4. Run development server: npm start
# 5. Build for production: npm run build
#
# With Docker:
# docker build -t chat-frontend .
# docker run -p 3000:3000 chat-frontend
